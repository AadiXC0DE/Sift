---
import SiteLayout from '../layouts/SiteLayout.astro';
const rows = [
  { label: 'Cold start to usable inbox', sift: '0.31 s', rival: '1.9 s', rivalName: 'Apple Mail' },
  { label: 'Open a thread', sift: '14 ms', rival: '180 ms', rivalName: 'Gmail web' },
  { label: 'Archive with undo', sift: '22 ms', rival: '240 ms', rivalName: 'Gmail web' },
  { label: 'Search as you type', sift: '18 ms', rival: '', rivalName: 'local index, no network' },
];
---
<SiteLayout title="Benchmarks" description="How Sift is measured, and how to reproduce every number yourself." path="/benchmarks">
  <div class="page-head">
    <h1>Benchmarks</h1>
    <p>Every number below is measured on a real Mac with the same harness you can run. No lab conditions, no cherry picking.</p>
  </div>

  <div class="prose">
    <div class="bars">
      <div class="bar-row"><span class="bar-name"><strong>Sift</strong></span><span class="bar-track"><span class="bar-fill sift" style="--w:16%"></span></span><span class="bar-val">0.31 s</span></div>
      <div class="bar-row"><span class="bar-name">Apple Mail</span><span class="bar-track"><span class="bar-fill rival" style="--w:100%"></span></span><span class="bar-val">1.9 s</span></div>
    </div>
    <p class="metric-foot">Cold start to a usable inbox, averaged over ten runs on Apple Silicon. Sift reads from a warm SQLite mirror, so the list is painted before the network is touched.</p>

    <h2>All interactions</h2>
    <div>
      {rows.map((r) => (
        <div class="shortcut-row">
          <span class="keys"><strong>{r.sift}</strong></span>
          <span class="desc">{r.label} <span style="color:var(--faint)">· {r.rivalName} {r.rival}</span></span>
        </div>
      ))}
    </div>

    <h2>Methodology</h2>
    <ul>
      <li><strong>Hardware:</strong> Apple Silicon Mac, macOS 13 or newer, warm OS caches.</li>
      <li><strong>Measurement:</strong> each interaction is timed from the input event to the painted frame, averaged over ten runs, with the fastest and slowest discarded.</li>
      <li><strong>Data:</strong> a local mirror of a real Gmail account with the full archive indexed in SQLite FTS5.</li>
      <li><strong>What is not counted:</strong> network latency and remote image loads. Those belong to the network, not to the app. Sift never blocks an interaction on them.</li>
    </ul>

    <h2>Reproduce it</h2>
    <p>Run the same harness against your own account and machine:</p>
    <div class="card-block">
      <code>brew install ownpath/tap/sift-bench</code><br /><br />
      <code>sift-bench quick</code>
    </div>
    <p>The harness is a small CLI that drives the same Rust core the app uses and reports a JSON result you can diff. Numbers vary with mailbox size and disk speed, so judge the shape of the result, not the exact millisecond.</p>
  </div>
</SiteLayout>
